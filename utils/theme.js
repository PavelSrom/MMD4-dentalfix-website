import { createMuiTheme } from '@material-ui/core'

export default createMuiTheme({
  palette: {
    primary: { main: '#52969f' }, // I'm bluuue da ba dee ba da ba, da ba dee ba da ba
    secondary: { main: '#f77e7f' }, // sexy salmon color <3 #f77e7f (stolen) / #f58e80 / other
    text: {
      primary: '#3c3c3c'
    }
  }
})
